# 港中深课程资料下载页

这是部署到 GitHub Pages 的前端站点。仓库根目录继续保存课程资料，站点构建时会扫描根目录生成资料清单。

常用命令：

```bash
npm run dev
npm run build
npm run generate:manifest
npm run package:archives
```

单文件下载使用 GitHub raw 链接，课程目录下载使用固定 Release `course-archives` 中的课程压缩包。
